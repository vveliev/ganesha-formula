# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: ganesha
  rootgroup: root
  config: '/etc/ganesha'
  service:
    name: ganesha
  subcomponent:
    config: '/etc/ganesha-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
